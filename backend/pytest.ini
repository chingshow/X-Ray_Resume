# pytest.ini
[pytest]
asyncio_mode = auto
# 因為測試函式都用了 async def，pytest 預設不支援非同步，加上這行之後就不需要在每個測試函式上面加 @pytest.mark.asyncio 標記，自動處理
testpaths = tests
# 告訴 pytest 只去 tests/ 資料夾找測試檔案，不要去掃描整個專案
pythonpath = . 
python_files = test_*.py
python_classes = Test*
python_functions = test_*
